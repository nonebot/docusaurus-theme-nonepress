const colors = require("tailwindcss/colors");
const defaultTheme = require("tailwindcss/defaultTheme");

module.exports = {
  theme: {
    extend: {
      // nonepress: {
      //   light: {
      //     theme: {
      //       DEFAULT: "#ff00ff",
      //     },
      //   },
      // },
    },
  },
  variants: {
    extend: {},
  },
  plugins: [],
};
